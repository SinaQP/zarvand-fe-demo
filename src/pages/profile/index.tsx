import { FC } from 'react';
import Layout from '../../components/layout';
import styles from './index.module.scss';

const Profile: FC = () => {
   return <Layout headerClassName={styles.header}>PROFILE</Layout>;
};

export default Profile;
