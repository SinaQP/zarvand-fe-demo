import { FC } from 'react';
import Layout from '../../components/layout';
import styles from './index.module.scss';

const Trade: FC = () => {
   return <Layout headerClassName={styles.header}>TRADE</Layout>;
};

export default Trade;
